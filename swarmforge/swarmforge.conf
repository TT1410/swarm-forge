# Format: window <role> <agent> <worktree> [task|batch] [extra-cli-args...]
# The squad branch starts one persistent visible squad leader.
# Transient invisible workers are spawned dynamically. Their default agent
# backends are configured in swarmforge/squad.conf, not in this file.
window squad-leader codex master task
