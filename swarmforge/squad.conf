max_transient_agents 5
# Transient workers inherit the squad-leader backend by default.
# Set this to codex, claude, copilot, grok, or squad-leader to override.
# This selects the tool used when a worker is spawned; it does not start agents.
transient_agent squad-leader
max_active_template hardener 1
max_active_template qa 1
max_active_group architecture 1 architecture-reviewer architecture-cleaner architect
